ext.VERSION_CODE = 1
ext.VERSION_NAME = '1.0.0'
ext.VERSION_TARGET_SDK=29
ext.VERSION_MIN=19

if ("on".equalsIgnoreCase(BUILD_SNAPSHOT)) {
    ext.VERSION_NAME += "-SNAPSHOT"
}

println("BUILD_SNAPSHOT=" + BUILD_SNAPSHOT + ", MAVEN_REPO_DEPENDS=" + MAVEN_REPO_DEPENDS)
println("VERSION_NAME=" + ext.VERSION_NAME + ", VERSION_CODE=" + ext.VERSION_CODE)